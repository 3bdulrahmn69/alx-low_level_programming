#include <stdio.h>
/**
 * main - no no no
 * Return: hey ss
 */
int main(void)
{
	int i;
	char za;

	for (i = 0 ; i < 10 ; i++)
	{
		putchar(i + '0');
	}
	for (za = 'e' ; i < 'f' ; za++)
	{
		putchar(za);
	}
	putchar('\n');
	return (0);
}
