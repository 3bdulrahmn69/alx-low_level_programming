#include <stdio.h>
/**
 * main - no no no
 * Return: hey ss
 */
int main(void)
{
	int i;
	char j;

	for (i = '0' ; i < '10'	 ; i++)
		putchar(i + '0');

	for (j = 'a' ; i <= 'f' ; j++)
		putchar(j);

	putchar('\n');
	return (0);
}
