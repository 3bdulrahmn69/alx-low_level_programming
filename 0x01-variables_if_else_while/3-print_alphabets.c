#include <stdio.h>
/**
 * main - heeeeeeeey
 * Return: heeeeeeeey
 */
int main(void)
{
	char sm;
	char bi;

	for (sm = 'a' ; sm <= 'z' ; sm++)
	{
		putchar(sm);
	}
	for (bi = 'A' ; bi <= 'Z' ; bi++)
	{
		putchar(bi);
	putchar('\n');
	}
	return (0);
}
