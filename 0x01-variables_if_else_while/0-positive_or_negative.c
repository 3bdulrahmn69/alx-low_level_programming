#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/**
 * main - hey in doc
 * Return: hey take the return
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if(a > 0)
	{
		printf("a is positive");
	}
	else if(a < 0)
	{
		printf("a is negative");
	}
	else
		printf("a is zero");
	return (0);
}
