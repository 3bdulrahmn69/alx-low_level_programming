#include "main.h"
/**
 * print_alphabet - Program enter point
 * Return: Program return
 */
void print_alphabet(void)
{
	char ph;

	for (ph = 'a' ; ph <= 'z' ; ph++)
		_putchar(ph);
	_putchar('\n');
}
